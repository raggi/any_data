load 'tasks/setup.rb'

ensure_in_path 'lib'
require 'any_data'

task :default => :test

PROJ.name = 'any_data'
PROJ.authors = 'James Tucker'
PROJ.email = 'raggi@rubyforge.org'
PROJ.url = 'http://github.com/raggi/any_data'
PROJ.rubyforge.name = 'libraggi'
PROJ.version = AnyData.version
